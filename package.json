{
  "name": "@very-coffee/monorepo",
  "type": "module",
  "workspaces": [
    "packages/*",
    "tools/*"
  ],
  "scripts": {
    "dev:app": "bun run --filter @very-coffee/app dev",
    "context": "bun run --filter @very-coffee/scripts context"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0"
  },
  "dependencies": {
    "convex": "^1.31.0"
  }
}
